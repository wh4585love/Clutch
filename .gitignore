# Dependencies
node_modules/
.pnpm-store/

# Build
dist/
build/
*.tsbuildinfo

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
services/orchestrator/.venv/
services/orchestrator/.rivet/
.ruff_cache/
*.egg-info/

# Environment
.env*
!.env.example

# Local credentials (must never ship in repo or DMG)
models.json

# Local agent / IDE settings (may contain personal paths)
.claude/settings.local.json

# POC run artifacts (machine-specific paths; regenerate locally)
experiments/pty_poc/runs/*
!experiments/pty_poc/runs/.gitkeep

# OS & logs
.DS_Store
*.log
.clutch-vite.pid
.clutch-vite-dev.log
coverage/
htmlcov/

# Layer 5 — 验证报告归档（保留目录占位）
runs/verification/*
!runs/verification/.gitkeep
!runs/verification/README.md

# E2E 报告（e2e/ 创建后，见 DECISIONS D1）
e2e/playwright-report/
e2e/test-results/

# Tauri (future)
apps/desktop/src-tauri/target/
apps/desktop/src-tauri/binaries/
services/orchestrator/build/
services/orchestrator/dist/
services/orchestrator/src/workflow_assets/*.json
.codegraph/
.clutch/
